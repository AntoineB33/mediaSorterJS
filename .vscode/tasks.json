{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Stop Node.js",
            "type": "shell",
            "command": "${workspaceFolder}\\stopNodeJS.bat"
        },
        {
            "label": "Invoke API",
            "type": "shell",
            "command": "Invoke-RestMethod -Uri http://localhost:3000/test -Method Post -Headers @{ \"Content-Type\" = \"application/json\" } -Body ''",
            "problemMatcher": []
        }
    ]
}
